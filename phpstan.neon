parameters:
    level: max
    paths:
        - %rootDir%/src/

    excludePaths:
        # Test dependencies
        - 'tests/Application/**/*'

    ignoreErrors:
        - identifier: missingType.iterableValue
